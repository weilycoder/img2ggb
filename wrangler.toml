name = "img2ggb"
main = "src/index.js"
compatibility_date = "2024-01-01"

# 静态资源目录
[assets]
directory = "./public"

[vars]
# 在这里配置环境变量，或使用 wrangler secret put 命令设置敏感信息
# AI_API_KEY = "your-api-key"

# 配置信任的源（逗号分隔）
# TRUSTED_ORIGINS = "https://example.com,https://app.example.com"
# 默认信任本地开发环境和 workers.dev 域名
